<Page x:Class="ShelterApp.Pages.AnimalsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="AnimalsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Список животных" FontSize="24" FontWeight="Bold" Margin="20"/>

        <DataGrid Grid.Row="1" x:Name="AnimalsDataGrid" AutoGenerateColumns="False" Margin="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Вид" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Порода" Binding="{Binding Breed}" Width="150"/>
                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="80"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
